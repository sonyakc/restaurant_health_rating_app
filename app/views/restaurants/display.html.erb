<!DOCTYPE html>
<html>
<head lang="en">
    <meta charset="UTF-8">
    <title>Search Results</title>
</head>
<body>
    <h3>Restaurant Search Results: <%= @restaurants.count %></h3>

    <table>
      <tr>
        <th>Restaurant</th>
        <th>Grade</th>
      </tr>

      <% @restaurants.each do |key, array| %>
          <tr>
            <td><%= array %></td>
            <td><%= array %></td>
          </tr>
      <% end %>
    </table>

    <br>
</body>
</html>